# 改善ロードマップ（第12回時点）

likes予測プロジェクトの今後の方向性。各項は **そのまま実装担当（別チャットのClaude等）に渡せる仕様書** として書いてある。
着手時はこのファイルの該当セクションを丸ごとプロンプトに貼ればよい。

- 策定日: 2026-07-07（第12回後）
- 前提知識: `CLAUDE.md` と `講義日報.md` を参照。特に「3段階リーク」「分布シフト」「時間相対比D2/D4」の知見。

---

## 全タスク共通の鉄則（違反したら結果は無効）

1. **リーク禁止**: views / dislikes / comment_count / video_error_or_removed は特徴量に使わない（投稿後に決まる量）。
2. **チャンネル系特徴は学習期間のみから算出**。全データ平均はリーク。
3. **評価プロトコル**: `english_titles.csv`（37,236行）→ video_id で重複除去（5,817件）→ trending_date 順の時系列分割。ランダム分割は禁止。
4. **主指標**: 回帰は R²/MAE、分類は 正解率/±1段階/マクロF1/低F1、順位は Spearman ρ。MAPEは小likes動画で爆発するので使わない。
5. **ベースライン**: 4段階分類 v1（acc 0.616 / ±1段階 0.911 / 低F1 0.125）、D2/D4（acc 0.47〜0.49 / ±1段階 0.90 / 低F1 0.39〜0.40）、順位 ρ=0.62。これを超えない改善は「効かなかった」と正直に記録する。
6. **sandbox運用**: pip は `--break-system-packages`。1方式3〜4秒程度なら前景実行でよい。バックグラウンド(setsid/nohup)は kill されることがある。ログは /tmp に書く。45秒制限が心配なら v3 方式の分割実行（`python3 xxx.py <method>` → `assemble`）を踏襲。
7. **記録**: 完了したら `講義日報.md` に追記、README の Phase と likes_rf_genre_log/ の表を更新。

---

## 方向性1: log-likes の加法分解モデル ★最優先・本命

**狙い**: `log(likes) = 時代効果 + チャンネル効果 + 内容効果 + ノイズ` を正面から分解し、
「時代・発信者・内容が各何%を説明するか」を1枚の分散分解表にする。

**なぜ筋が良いか**: C(チャンネル相対比)とD(時間相対比)は、実はこの構造の部分修正を別々にやっているだけ。
統一すると、これまでの知見（発信者支配・新規ch予測不能・分布シフト）が全部この1つの表に統合され、
報告書の最終章として最も強い。

**仕様**:
- 段階的残差回帰で実装する:
  1. 時代効果 = D4方式の平滑週次水準（学習期間の週次中央値、test週は最終水準持ち越し）を log で引く。
  2. チャンネル効果 = 残差に対するチャンネル別平均（学習期間のみ。方向性2の縮小推定を使うとなお良い）を引く。
  3. 内容効果 = 残り残差を投稿時点特徴（タイトル・タグのTF-IDF、title統計、publish_hour/dow、category_id）でRF回帰。
- 各段階で test の分散をどれだけ削ったかを記録し、寄与率(%)の表を作る。
- カテゴリ別にも同じ分解を出す（Sports易/Gaming難の「なぜ」が見える。方向性5と接続）。

**合格基準**: 寄与率の表が出ること。内容効果の残差R²が0を超えるか（超えなければ「内容は時代とチャンネルを
統制すると説明力ゼロ」という強い結論として書く）。

**成果物**: `likes_rf_genre_log/likes_decompose.py` + 寄与率CSV + カテゴリ別図。

---

## 方向性2: チャンネル効果の縮小推定（経験ベイズ）— コールドスタートの根治

**狙い**: チャンネル平均likesを「本数nに応じてカテゴリ平均へ縮小」する部分プーリングに置き換える。

**なぜ筋が良いか**: fame_split（第11回）の有名/中堅/無名/新規という4区分は粗く、新規chはAUC0.602で崩壊していた。
縮小推定なら `(n・ch平均 + k・カテゴリ平均) / (n + k)` の1本の式で、新規chは自動的にカテゴリ平均へ
フォールバックし、1本しかないchの過学習も防げる。原理的な処方箋。

**仕様**:
- 特徴量 `channel_mean_likes_log` を縮小版に置換。k はハイパーパラメータ（1, 3, 5, 10, 30 を学習期間内の
  時系列内部分割でチューニング。testで選ぶのは禁止）。
- カテゴリ平均も学習期間のみから算出。
- 比較: 現行（fillna=全体平均）vs 縮小版。全体指標に加えて fame_split の4群別（特に新規・無名群）の
  AUC/Spearman を必ず出す。
- チャンネル集計は「その動画の投稿時点より前」に限定する厳密版も同時に検証する（従来TODOの吸収）。

**合格基準**: 新規・無名群の AUC/ρ が現行および content-only（新規群AUC 0.281相当）を上回るか。

**成果物**: `likes_rf_genre_log/likes_shrinkage.py` + 4群別比較表。

---

## 方向性3: Conformal Prediction による最低保証の理論武装

**狙い**: `predict_floor()` の conf（経験的な安全率）を split conformal prediction に置き換え、
「保証成立率 ≥ 90%」を分布仮定なしの理論保証にする。

**なぜ筋が良いか**: 現状のconfは「やってみたら95%だった」という事後観察。conformalなら較正データで
分位点を較正するだけで、有限サンプルのカバレッジ保証が理論的に付く。順序クラスの下限予測と相性が良く、
実装も軽い。「経験則→統計的保証」への格上げは講義の締めとして強い。

**仕様**:
- 学習期間の末尾20%を較正セットに割く（時系列順を維持: 学習64% / 較正16% / test 20%）。
- 非適合度スコア = 真の段階の予測確率に基づく標準的なもの（ordinal用に累積確率版も試す）。
- 分布シフト対策として較正窓を「直近Nどれだけに限定するか」を比較（全較正期間 vs 直近8週 vs 直近4週）。
  ※これ自体が「シフト下でconformalの保証がどこまで持つか」という検証テーマになる。
- ターゲットは v1 と D4 の両方で実施。
- 出力は predict_floor 互換: (下限段階, 保証水準)。

**合格基準**: 指定カバレッジ（90%/95%）を時系列 test で実測達成するか。较正窓の長さとカバレッジの関係の図。

**成果物**: `likes_rf_genre_log/likes_conformal_floor.py` + カバレッジ検証表・図。

---

## 方向性4: 週内ランキング学習（分母問題の消去）

**狙い**: likesの水準予測をやめ「同じ週にトレンド入りした動画の中での相対順位」を当てる問題に変える。

**なぜ筋が良いか**: 週内で比較する限りインフレは定義上消える。D1〜D4で苦労した分母の設計問題が
丸ごと不要になる、いちばんエレガントな解。

**仕様**:
- ペアワイズ分類: 同一週の2動画のペアを作り「どちらのlikesが多いか」を二値分類（特徴量は両動画の差分または結合）。
  ペア数が爆発するので週ごとに上限サンプリング（例: 週あたり2,000ペア）。
- 評価: test期間の週内Spearman ρ（週ごとに算出して平均）とペア正解率。
- ベースライン: 既存モデルのスコアで週内順位を付けた場合の週内ρ（これに勝たないと意味がない）。
- 発展（時間があれば）: LightGBMのlambdarank。まずはRF/GBDTのペアワイズで十分。

**合格基準**: 週内ρが既存モデル由来の週内ρを上回るか。全期間ρ=0.62との比較も報告。

**成果物**: `likes_rf_genre_log/likes_pairwise_rank.py` + 週別ρの分布図。

---

## 方向性5: レジーム転換の解剖（分析系・懐疑チェック）

**狙い**: 2018年3月頃のlikes水準ジャンプは「本当のインフレ」か「見かけ」かを判定する。

**なぜ筋が良いか**: もし収集方法の変化やカテゴリ構成比のシフトによる見かけなら、分母をカテゴリ別に
持つだけでD方式がさらに改善する。どちらの結論でも報告書に書ける（結果が保証された調査）。

**仕様**:
- 週ごとに分解: (a) トレンド入り件数、(b) カテゴリ構成比、(c) カテゴリ内likes中央値、(d) 新規ch比率。
- ジャンプが「構成比シフト」（likesの多いカテゴリが増えた）か「全カテゴリ一様の上昇」かをシフト分解
  （likes中央値の変化 = Σ 構成比変化×カテゴリ水準 + Σ 構成比×カテゴリ内水準変化）で切り分ける。
- 一様上昇なら: YouTube側のトレンドアルゴリズム変更等の外因の可能性を報告書に注記。
- 構成比シフトなら: 分母をカテゴリ別週次水準にした D5 を likes_4class_v3.py に追加して再評価。

**合格基準**: シフト分解の図1枚と結論1文。（D5を作った場合は7方式比較表の更新）

**成果物**: `likes_rf_genre_log/explore_regime_shift.py` + 分解図。

---

## 方向性6: rolling-origin 評価基盤（全方向性の土台）

**狙い**: 現在の80/20一発分割を、学習窓をスライドさせる walk-forward 評価に格上げし、全指標に誤差幅を付ける。

**なぜ筋が良いか**: 「D4は境界がどこでも堅牢か」「レジーム転換をまたぐ分割で何が起きるか」に答えられる。
分母を週次更新する実運用のシミュレーションも兼ねる。1〜4のどれもこの上に載せると説得力が段違い。

**仕様**:
- 分割: 学習開始は固定（expanding window）、test窓は4週ずつ後ろへスライド。5〜6 fold 程度になるはず。
- 各foldで v1 / D4 の4段階分類を評価し、指標を fold 別に記録 → 平均±範囲で報告。
- D4の分母（最終水準）はfoldごとに更新（=週次更新運用のシミュレーション）。
- 計算量に注意: 1 fit 3〜4秒 × 2方式 × 6fold ≈ 40〜50秒。分割実行対応にする。

**合格基準**: fold別指標の表と「境界位置に対する頑健性」の図。レジーム転換をまたぐfoldの挙動に言及。

**成果物**: `likes_rf_genre_log/likes_rolling_eval.py` + fold別表・図。

---

## 追補: 追加のアイデア7〜12（優先度は6方向より下だが独立価値あり）

### 方向性7: 順序を使う — ordinal分類 / 分位点回帰への置き換え ★安い割に効くはず
現行RFは4段階を**名義**クラスとして扱っており、「低と高を間違える」のと「低と中低を間違える」のを同じ重さで学習している。
- 案A(ordinal): P(y>低), P(y>中低), P(y>中高) の3つの二値分類器に分解し、累積確率から段階を決める。
- 案B(分位点回帰): GBDTのquantile lossでlog-likesの条件付き分位点(10/25/50/75/90%)を直接予測し、
  段階は閾値切りで導出。**下位分位点がそのまま最低保証になり、4段階・floor・conformal(方向性3)が1モデルに統一される。**
- 合格基準: ±1段階と低F1がD4ベースライン比で改善するか。
- 成果物: `likes_ordinal.py`。ターゲットはD4相対比で実施。

### 方向性8: 捨てているデータを使う — トレンド滞在日数・軌跡
重複除去で37,236行→5,817件に落としているが、**捨てた重複は「その動画が何日トレンドに滞在したか」
「日ごとにlikesがどう伸びたか」という追加情報**を持っている。
- 別ターゲット: トレンド滞在日数の予測（投稿前特徴のみ・リークなし。likesより頑健な人気指標かもしれない）。
- 分析: 初日likesと最終日likesの伸び率の分布。likesターゲット自体を「初日」でなく「最終観測日」に
  変えた場合の各手法の成績比較（現在は暗黙に「初日スナップショット」を予測している。この選択自体を明示・検証すべき）。
- 合格基準: 滞在日数予測のR²/ρ、およびターゲット定義変更の影響表。

### 方向性9: 選択バイアスの明示（分析・報告書向け）
このデータは「トレンド入りした動画」しか含まない。つまり全モデルは **P(likes | トレンド入り)** を学習しており、
「投稿前にlikesを予測する」という看板とは条件付けが違う。チャンネル実績が支配的なのも
「トレンド入りの選抜を通る力」の代理変数である可能性が高い。報告書に1節、限界として明示する価値がある。
コスト極小で誠実さが上がる。方向性5(レジーム解剖)と同じスクリプトで裏取り可能。

### 方向性10: LLMを特徴量抽出器に使う（コールドスタートの内容側テコ入れ）
TF-IDFでは拾えない意味特徴を、LLMでタイトル+タグ+概要欄からアノテーションする:
クリックベイト度(0-10)、トピック分類、感情、有名人言及の有無、シリーズ物か、など。
ユニーク5,817件ならバッチ処理で現実的なコスト。**新規チャンネル(内容しか手がかりがない群)のAUC 0.6を
どこまで上げられるか**が評価の焦点。効かなければ「内容は本当に説明力がない」というより強い結論になる。
- 注意: アノテーションはタイトル等の投稿前情報のみに基づくこと（likesを見せてはいけない）。
- 成果物: 特徴量CSV + fame_split新規群での比較。

### 方向性11: 他国データでの増強・転移
Kaggle元データにはUS以外(GB/CA等)も同時期のトレンドデータがある。内容効果の学習を多国データで安定させ、
評価はUSのみで行う転移設定。データが5倍になり、特に内容特徴の係数が安定するはず。
チャンネル効果は国をまたいで共有されるか、という問い自体も面白い。
- 注意: 同一動画が複数国でトレンドする重複に注意（video_idで国横断の重複除去が必要）。

### 方向性12: チャンネル効果の時間減衰
チャンネル平均likesを全学習期間の平均でなく**指数減衰つき加重平均**（直近の実績ほど重い）にする。
レジーム転換があるデータでは古い実績は水準がズレている。方向性2(縮小推定)に1パラメータ足すだけで済むので、
2の実装時に同時に検証するのが効率的。conformal(方向性3)側でもカテゴリ別較正(Mondrian conformal)という
同種の拡張があり、これも1行で言及しておく。

## 推奨の進め方

| 順序 | タスク | 理由 |
|---|---|---|
| 先 | 6（評価基盤） | 以降の全結果に誤差幅が付く |
| 主戦 | 1 + 2（分解＋縮小推定） | 知見の統合。報告書の最終章になる |
| 仕上げ | 3（conformal） | 実用側の締め。理論保証つき最低保証 |
| 並行可 | 4、5 | 独立性が高く別セッションに切り出しやすい |
| 安い割に効く | 7（ordinal/分位点） | D4実装時に一緒に検証すると効率的 |
| 面白枠 | 8（滞在日数）、10（LLM特徴量） | 新しいターゲット/新規ch対策。結果がどちらでも報告書ネタになる |
| 報告書用 | 9（選択バイアス） | コスト極小。5と同時に |
| 余力があれば | 11（他国転移）、12（時間減衰） | 12は方向性2に同梱 |

各タスク完了時: `講義日報.md` 追記 → README更新 → 必要なら報告書に反映、の順（CLAUDE.md「運用」参照）。
