# 講義日報

毎回の講義で「やったこと・分かったこと・次にやること」を記録する。新しい回は **このすぐ下に追記**（新しい順）。各エントリは下のテンプレートをコピーして使う。

---

<!-- ▼▼▼ 新しい回はこのテンプレートをコピーして上に貼る ▼▼▼

## YYYY-MM-DD（第N回）

### 今日やったこと
-

### 分かったこと / 結果
-

### つまずき・疑問
-

### 次にやること
-

▲▲▲ ここまでテンプレート ▲▲▲ -->

## 2026-07-21（第13回）

### 今日やったこと
- 1回欠席のため「最短の実験数でDLまで到達する」計画に圧縮:
  実施=方向性3(conformal)+DL検証の2本、切り捨て=方向性4・8・11・12、
  方向性5+9は時間があれば、CLIP等の事前学習マルチモーダルはローカル実行枠へ。
- 方向性3: `likes_conformal_floor.py` を実装。rolling-origin全6foldで、学習窓末尾約20%を
  較正セットに割く split conformal。非適合度スコア s=P̂(y≤y_true)、
  τ=⌊α(n+1)⌋番目の順序統計量。v1/D4両ターゲット×較正窓3種×α∈{0.10,0.05}。
- DL検証: `likes_dl_text.py` を実装。numpyスクラッチのDAN（埋め込み64d平均プーリング
  →128→64→softmax、Adam・dropout0.3・クラス重み付きCE）。TF-IDF+RF内容ベースラインと
  同一プロトコル比較+概要欄アブレーション。
- 事前学習マルチモーダル（sentence-transformers+CLIPサムネイル）はsandboxのネットワーク
  遮断（HuggingFace/ytimg/PyTorchミラーすべて403）で実行不可 → ローカル実行用
  `run_local_multimodal.py` を整備（text_embed/download/clip_embed/evaluateの4段階）。
- 班員の「12~13週目 報告」（ViTサムネイル特徴+RFで likes回帰 R²=0.64）をレビュー。
  重複リークの疑いを検証するため「チャンネル過去平均だけ」ベースラインを実装
  （`likes_channel_baseline.py`。A=班員と同条件の再現 / B=正規プロトコル の2本立て）。
- 班員の「12~13週目 報告」CSV（`thumbnail_features_1000.csv`）の再利用を本採用決定。
  これはViTの768次元ベクトルではなく Google Vision API Web Detection の出力
  （extracted_text=OCR / labels=画像ラベル / web_entities=固有名詞）。軽量・解釈可能で
  投稿前サムネイル内容の代理としてリークなしで使える。3列を1テキストに結合しTF-IDF化する
  正式モジュール `likes_thumb_meta.py` を作成（全5,817件が揃えばrolling-originに自動切替）。
- 「もっとシンプルなモデル」の検証（`likes_simple_models.py`）: 分散分解の
  チャンネル31.5%/時代11.9%/内容6.8%を根拠に、内容(TF-IDF)を削ってどこまで保つか。
  L1_formula（学習なし・縮小chan平均÷トレンド水準を段階化）/ L2_rf6（特徴6個のRF）/
  L3_logit2（特徴6個の二値ロジスティック回帰）を現行フルと同一プロトコルで比較。
- 「具体的なlikes数値」の予測器を実装（`likes_numeric_pred.py`）:
  CH_only（縮小チャンネル平均のみ）/ RF_log（log直接回帰）/ RF_ratio（log相対比回帰→
  D4分母掛け戻し、本命）の3方式 + RF_ratioにconformal 90%予測区間（較正log残差の
  分位点による乗法区間）。rolling-origin全fold・conformalと同じ学習/較正/test分割。

### 分かったこと / 結果
- **カテゴリ別モデルは「全体では効かない・一部カテゴリでは効く」**（`likes_category_models.py`、
  fold0-4平均）: 分母もモデルもカテゴリ別にしたC_model は全体 log R² が悪化（0.285→0.268）。
  カテゴリごとに学習データを分割すると小データ+TF-IDF300次元で過学習し、カテゴリ横断の
  共通学習の恩恵を失うため。分母だけカテゴリ別(C_denom)は R²微増(0.289)・ρ微減でほぼ引き分け。
  ただしカテゴリ別 log R²で ScienceTech(+0.182)・Comedy(+0.088)・Education(+0.064)は明確に改善、
  HowtoStyle(-0.183)・News(-0.115)は悪化。→「全カテゴリ専用化」でなく「効くカテゴリだけ
  選択的に専用モデル」が正しい使い方。Sports易(R²0.40台)/HowtoStyle・News難(0近辺)を再確認。
- **方向性12（チャンネル効果の時間減衰）は効かなかった**（`likes_channel_decay.py`、fold0-4平均）:
  指数減衰加重（半減期H12/H6週）は等重み平均(inf=現行)を全指標で超えず、減衰を強めるほど悪化。
  狙い撃ちした薄いch群(実績1〜2本)でも inf が最良(ρ0.692 vs H6 0.641)で仮説は明確に棄却。
  理由: 縮小推定(k=1)が既に薄いchをカテゴリ平均へ寄せており、少実績の重み付け変更は情報を
  増やさない。副産物: strict版(各動画の投稿週より前だけ集計)は log R² が高い(0.335 vs 0.285)。
  第12回「厳密リーク排除は効かない(分類)」とは逆で、数値回帰では厳密版が素直という新発見。
- **conformal合格基準達成**: α=0.10でカバレッジ平均0.93、全foldでmin≥0.905と理論通り。
  95%版も達成。無較正(τ=α)はfoldにより0.882へ割れ、かつ下限が浅い
  （mean_floor 0.91 vs conformal 1.22）→「conformalの方が安全かつ情報量が多い」。
  方向性7の伏線（quantile カバレッジ77.4%）をきれいに回収。
- 較正窓は**全較正期間が正解**。直近2週に絞るとサンプル不足で保証が壊れる（min 0.882）。
  「分布シフト対策の適応的較正窓」はこのデータ規模では不要だった。
- **スクラッチDLはRFに勝てず**（fold0-4平均）: 同一入力でDAN acc 0.443 vs RF 0.454、
  低F1 0.420 vs 0.455、新規ch ρ 0.269 vs 0.332。フル構成でも0.473 vs RF_full 0.477。
  概要欄はDAN内では効く（新規ch ρ 0.269→0.321）がRF水準止まり。
  → 内容の説明力の薄さは**モデル表現力でなく情報自体の限界**の可能性がさらに強まった
  （Phase10の内容効果6.8%と整合）。「効かなかった」と正直に記録。
- sandboxの環境制約の記録: HuggingFace・ytimg・download.pytorch.orgは403で遮断。
  PyPI経由のpipは通る。DLをやるならnumpyスクラッチ（DAN程度なら1fold6秒）か、ローカル実行。
- **ViTのR²=0.64はほぼ確実に重複リーク**: 班員と同条件（重複除去なし・ランダム80/20・
  IQR外れ値除去）で、画像もテキストも使わない「チャンネル平均1個」の予測が
  n=1000: R²0.21 / n=3000: 0.48 / n=6000: 0.56 / n=10000: 0.61 とViT（0.005/0.39/0.64/0.66）と
  ほぼ同水準。しかもtest中の重複動画率（21%→46%→64%→79%）とR²上昇が完全に並走
  → 「データ増で精度向上」ではなく「testに学習済み動画が混ざる率の上昇」が正体。
  ViTの0.64は暗記+チャンネル情報の代理（同一chのサムネは似ている）でほぼ説明がつく。
  第10回のリーク2（動画重複）+リーク3（ランダム分割）と同型。
- 正規プロトコル（重複除去・時系列fold、fold0-4平均）でのチャンネル平均だけの成績:
  raw R² 0.496 / log R² 0.142 / Spearman ρ 0.553。**画像特徴が超えるべき真の下限**。
  ViT埋め込み（pkl）を貰えれば正規プロトコルでの再評価が可能（次の宿題）。
- **サムネVisionメタは「使える形式」だが811件では効果は微少**（時系列70/30単発）:
  ρ で サムネメタのみ0.088（単体は無力）、アカウント+時間0.354、+サムネメタ0.368（最良）、
  +タイトル/タグ0.350、全部載せ0.357。サムネメタがタイトル/タグをわずかに上回るが誤差の範囲。
  R²が全体マイナスなのは811件と少なすぎるため。**全5,817件のメタ取得が本採用の前提条件**
  （班員のVision APIパイプラインで取得。ViT 768次元pklより軽量で報告書向き）。
- **シンプル化しても精度はほぼ落ちない**（fold0-4平均、現行フル=acc0.477/±1段階0.851/低F10.423）:
  L1（学習なしの数式のみ）ですら acc0.455/±1段階0.894/低F10.443 と現行とほぼ互角で
  ±1段階はむしろ上。L2（特徴6個RF）が4段階では最良バランス（acc0.469/±1段階0.897/低F10.463、
  低F1と±1段階は現行超え）。L3（二値ロジスティック）はAUC0.808（第11回フル二値0.86には届かず）。
  → TF-IDF300次元・DLは説明力に対しコスパの悪い飾りだったと実証。「チャンネル平均÷トレンド水準」
  という1本の比が骨格で、特徴6個を足せば現行に並ぶ。推奨は L2（軽い・解釈可・性能維持）。
- **数値予測の実力を定量化**（fold0-4平均）: 最良のRF_ratioでも log R² 0.120
  （fold依存で-0.33〜0.36、レジーム転換をまたぐfold2はマイナス）、中央誤差(MdAPE) 64%、
  ρ 0.610。CH_only(0.014)・RF_log(-0.021)をRF_ratioが上回り「時代正規化してから回帰」が
  数値予測でも正解。**順位は当たるが絶対数は±64%が実力**。
- conformal 90%予測区間はカバレッジ0.893とほぼ設計通りに機能するが、**幅が平均171倍**
  （予測値の約1/13〜13倍）。「予測1万→90%区間は約800〜13万」が誇張なしのデータの実力。
  → 数値を出すなら「点予測(参考値)+90%区間+段階分類」の3点セットが誠実な運用形。
  第10回の「投稿前のlikes絶対数予測は難しい」を予測区間の形で定量的に裏付けた。

### つまずき・疑問
- 較正窓の設計はロードマップ原案（全 vs 8週 vs 4週）から全 vs 4週 vs 2週に変更した
  （最短学習窓10週では8週較正だと学習データが残らないため）。
- 事前学習表現（MiniLM/CLIP）で内容効果が変わるかは未検証のまま残った。
  `run_local_multimodal.py` をMacで回せば同一プロトコルで白黒つく（ダウンロード込み30分目安）。

### 次にやること
- DL章はスクラッチDANの結果で終結と決定（ローカル実行はしない方針）。事前学習
  （MiniLM+CLIP、`run_local_multimodal.py` 整備済み）は「今後の課題」として報告書に1節記載。
  実行する場合の合格ラインは RF_content の acc 0.454/低F1 0.455/新規ch ρ 0.332 超え。
- 報告書に「理論保証つき最低保証（conformal）」と「DL検証（スクラッチDAN）」の章を追記。
- 余力があれば方向性5+9（レジーム解剖+選択バイアス明記、学習なしの集計のみ）。

## 2026-07-07（第12回）

### 今日やったこと
- 行き詰まり相談から方向性を再整理。「精度の壁」ではなく「問題設定の壁」と診断し、
  最有力の「1. ターゲットの時間正規化」→「5. 報告書化」の順で実行。
- 時間相対比ターゲット（方式D）を検証する `likes_4class_v3.py` を作成。
  likes を「その時期のトレンド水準」で割った比でビンを切り、v1/A/C と同一条件で比較。
- 分母の作り方を4方式比較: D1=週の実中央値（オラクル）/ D2=学習全期間のlog線形外挿 /
  D3=学習直近8週のみで外挿 / D4=学習最終水準の持ち越し（ランダムウォーク予測）。
- 結果を中間報告書続編に第5章として追記し、結論・要旨・今後も更新（docx/PDF再生成）。
- 今後の改善6方向を実装仕様書つきで `改善ロードマップ.md` に策定
  （実装は別チャットのOpus/Sonnetに該当セクションを貼って委任する運用）。
- さらにアイデアを絞り出し、追補7〜12をロードマップに追加:
  7=ordinal/分位点回帰（4段階・最低保証・conformalを1モデルに統一、本命級）/
  8=捨てた重複31,419行の再利用（トレンド滞在日数・日次軌跡。現ターゲットが
  「初日スナップショット」である暗黙の選択も明示）/ 9=選択バイアスの明示
  （全モデルは P(likes|トレンド入り) を学習している）/ 10=LLM特徴量抽出（新規ch対策）/
  11=他国データ転移 / 12=チャンネル効果の時間減衰。
- セッション全体の記録を `メモ_20260707_セッション記録.md` に保存。

### 分かったこと / 結果
- **7方式比較（test）**: リークフリーの **D2（acc 0.489 / ±1段階 0.905 / 低F1 0.400）** と
  **D4（acc 0.467 / ±1段階 0.903 / 低F1 0.390）** が「低」を v1（低F1 0.125）の3倍超に回復。
  マクロF1・±1段階は v1 同等を維持し、C（acc 0.344 / ±1段階 0.611）を全指標で上回った。
  → 「スケールだけ補正して問題は難化させない」という D の狙いが成立。
- **発見1: 分母は正確さより滑らかさ**。オラクルの D1（実週中央値）は acc 0.355 と D2 より悪い。
  週の実中央値は「同時期の競合」というノイズを目的変数に持ち込むため学習可能性が下がる。
- **発見2: likesインフレはレジーム転換**。週次中央値は log線形ではなく 2018年3月頃に
  一段ジャンプする。全期間外挿（D2）は過小評価（×0.58）、直近8週外挿（D3）は
  過大評価（×2.55）で acc 0.204 に崩壊。転換後が横ばいの本データでは持ち越し（D4）が堅実。
- 用途の使い分け: 「低」込みの安定4段階なら D2/D4、ピタリ正解率最重視なら v1。
- **現時点のベスト手法の結論**: 総合はD2（数値ベスト）、運用堅牢性込みならD4。
  外せない約束には predict_floor()（conf=0.7で保証95%）、二値なら AUC 0.86。
  正式一本化の本命は「D4ターゲット + predict_floor 接続」。

### つまずき・疑問
- sandbox が新セッションでライブラリ再インストールが必要だった（pip --break-system-packages）。
- setsid/nohup のバックグラウンド実行が途中で kill される事象。実測 1方式3.5秒と軽く、
  前景実行で問題なし（過去の「40秒台」はポーリングの待ち時間を誤認していた可能性）。
- v3 は45秒制限対策として方式ごとの分割実行（`python3 likes_4class_v3.py <method>` → `assemble`）に対応。

### 次にやること（当初）
- [x] **`改善ロードマップ.md` の6方向を推奨順に実行**: 6(rolling-origin評価基盤) →
      1(log-likes加法分解)+2(チャンネル効果の縮小推定) → 3(conformalで最低保証)。
      4(週内ランキング)・5(レジーム転換の解剖)は並行可。→ 同日中に6・2・1・7(追補)に着手（下記続き）。
- [ ] チャンネル特徴の「投稿時点より前のみ」厳密集計はロードマップ方向性2に吸収済み。
- [ ] 未活用の投稿前特徴（description・サムネイル）の追加検討（前回からの持ち越し・優先度低）。

### 生成したファイル（前半）
- `likes_rf_genre_log/likes_4class_v3.py`（時間相対比D1〜D4の検証・分割実行対応）
  - `likes_4class_v3_compare.csv` / `likes_4class_v3_report.txt`
  - `likes_4class_v3_confusion.png` / `likes_4class_v3_trend.png`
- `G5中間報告書_続編.docx` / `.pdf`（第5章「時間相対比による分布シフトの補正」を追記、旧5章→6章）
- `改善ロードマップ.md`（主要6方向+追補6件=計12方向の実装仕様書。共通の鉄則・合格基準・成果物定義つき）
- `_report_edit_gen_section.py`（報告書docxのXML編集スクリプト。再編集用・gitignore対象）
- `メモ_20260707_セッション記録.md`（本セッションの記録・別チャット参照用）
- `README.md` に Phase7 とスクリプト表の行を追記

### 今日やったこと（続き・同日後半）
- `改善ロードマップ.md` の推奨順に沿って着手。まず土台となる方向性6「rolling-origin評価基盤」
  を実装（`likes_rolling_eval.py`）。従来の80/20一発分割（trending_date順）を、
  学習開始固定・学習窓を4週ずつ伸ばしながらtest窓を4週スライドさせる expanding window評価に格上げ。
- 最小学習窓10週・test窓4週で全6 foldを構成し、各foldで v1（学習分位点・現行）と
  D4（時間相対比・最終水準持ち越し）を比較。D4の分母（最終水準）はfoldごとに学習期間のみ
  から再計算し、週次更新運用のシミュレーションを兼ねた。
- sandbox 45秒対策として v3 同様に分割実行対応（`python3 likes_rolling_eval.py <fold_id>` →
  `assemble`）。全6foldを順に実行しassemble。

### 分かったこと / 結果
- **fold別の指標（v1 / D4）**:

| fold | test週範囲 | v1 acc | v1 低F1 | D4 acc | D4 低F1 |
|---|---|---|---|---|---|
| 0 | 10-13 | 0.416 | 0.415 | 0.421 | 0.444 |
| 1 | 14-17 | 0.535 | 0.507 | 0.540 | 0.469 |
| 2 | 18-21 | 0.610 | 0.053 | 0.473 | 0.230 |
| 3 | 22-25 | 0.611 | 0.182 | 0.411 | 0.337 |
| 4 | 26-29 | 0.653 | 0.326 | 0.540 | 0.634 |
| 5 | 30(部分,n=40) | 0.650 | 0.000 | 0.325 | 0.333 |

  平均: v1 acc=0.579(0.416〜0.653) / 低F1=0.247(0.00〜0.507)、
  D4 acc=0.452(0.325〜0.540) / 低F1=0.408(0.23〜0.634)。
- **発見1: 単発80/20評価は境界依存が大きい**。accが最小0.416〜最大0.653まで±0.24近くブレる。
  第12回までの「v1 acc=0.616」は複数ある妥当な結果のうちの1点に過ぎない。
- **発見2: 「D4は低F1でv1を上回る」は6fold全体でも再現・頑健**（低F1平均0.408 vs 0.247）。
  単一分割の結果ではなく、境界を変えても成立する構造的な差だと確認できた。
- **発見3: レジーム転換の影響が可視化された**。2018年3月頃の転換週（week≈17）をまたぐ
  fold1、またいだ直後のfold2・3でv1の低F1が0.507→0.053→0.182と大きく崩れる一方、
  D4は0.469→0.230→0.337と崩れ幅が小さい。転換直後は両手法とも苦戦するが、D4の方が
  「低」を拾い続けられる度合いが高い。
- 最終foldはデータ収集終了で該当週の件数が40件と小さく、参考値扱い（低F1が0/0.333と不安定）。

### 方向性2: チャンネル効果の縮小推定（経験ベイズ）— 続けて着手
- `channel_mean_likes_log`（現行=学習期間平均、無ければ全体平均でfillna）を経験ベイズ収縮
  `shrunk=(n_c・ch_mean_c + k・cat_mean)/(n_c+k)` に置換（`likes_shrinkage.py`）。
  n_c=0（完全新規チャンネル）は自動でカテゴリ平均へフォールバック。
  kは学習期間内側のtime-split検証で選定（候補1/3/5/10/30→k=1採用、testは不使用）。
- `likes_fame_split.py`と同じ知名度4群（有名/中堅/無名/新規）で
  current（現行）/ shrink（収縮）/ shrink_strict（学習データも投稿より前の実績のみに厳密化）/
  content（内容特徴のみ）の4モデルを比較。

| 群 | current spearman/AUC | shrink spearman/AUC | content spearman/AUC |
|---|---|---|---|
| 新規（学習期間に無し, n=220） | 0.125/0.602 | **0.363/0.751** | 0.281/0.761 |
| 無名（1本, n=99） | 0.588/0.790 | **0.601/0.820** | 0.183/0.657 |
| 中堅（2-5本, n=315） | 0.751/0.947 | 0.748/0.944 | 0.524/0.876 |
| 有名（6本+, n=530） | 0.690/0.883 | 0.696/0.877 | 0.532/0.843 |

- **合格基準達成**: 新規・無名群でshrinkがcurrent/content両方を上回った
  （新規はSpearman 0.125→0.363と約3倍、AUCもcontent-only水準まで回復。無名はcurrentを僅かに上回りつつ
  content比では大幅改善）。中堅・有名群の実力はcurrentと同等を維持し、どちらも失わない。
- **意外な発見**: 学習データも「投稿より前の実績のみ」に厳密化したshrink_strictは、
  全群でcurrent・shrinkより明確に悪化（新規0.289、全体spearman 0.459）。
  学習時に将来の同チャンネル実績まで参照できる（学習内の軽い自己参照）方がRFが
  チャンネル特徴を学習する上でむしろ有効に働いており、厳密にリークを排除すると
  かえって性能が落ちるという逆説的な結果になった。
  → 「従来TODO（投稿時点より前のみの厳密集計）」はやってみた結果「効かなかった」と
  正直に記録する（ロードマップの鉄則5に従う）。

### 方向性1: log-likes加法分解（時代+チャンネル+内容の分散分解）— 続けて着手
- `log(likes) = 時代効果(D4) + チャンネル効果(方向性2の収縮推定) + 内容効果(RF) + ノイズ`
  の段階的残差回帰を実装（`likes_decompose.py`）。C(チャンネル相対比)とD(時間相対比)が
  別々にやっていた補正を1つの分散分解表に統合する狙い。
- **設計上のつまずきと修正**: 当初は既存の80/20一発分割で実装したところ、時代効果(D4)の
  寄与率が常に0.0%になった。原因を調べると、D4のtest側は「学習最終水準の定数持ち越し」
  なので、test期間内では時代効果が定数になり、Var(X-定数)=Var(X)より数学的に必ず
  分散を削れない（分解手法の欠陥ではなく「1回きりのtest期間では時代水準が定数」という
  当然の帰結）。→ 方向性6のrolling-origin基盤（fold0〜4）をプールし、fold間で時代水準が
  変動する状態で分散分解する方式に切り替えて解決。sandbox 45秒対策も兼ねて
  fold毎の分割実行→assembleに対応。

### 分かったこと / 結果（方向性1）
- **全体の分散分解（fold0〜4プール・n_test=3079）**:

| 段階 | 累積R² | 寄与率(%) |
|---|---|---|
| 時代効果(D4) | 0.119 | 11.9 |
| +チャンネル効果(収縮推定) | 0.434 | 31.5 |
| +内容効果(RF) | 0.502 | 6.8 |
| 未説明(ノイズ) | - | 49.8 |

  内容効果の残差R²(out-of-sample, pooled)=0.116で**合格基準達成**（内容特徴に
  説明力が残る）。ただしチャンネル効果(31.5%)は内容効果(6.8%)の4倍以上あり、
  「誰が投稿したか」の支配性を分散分解の数値としても裏付けた。
- **カテゴリ別の分解**: Sports(内容R²=0.215)・Education(0.210)・Music(0.171)は
  内容特徴で説明しやすく、Gaming(0.036)・News&Politics(-0.177・content負)は
  内容特徴がほぼ効かない。従来「定性的」だった「Sports易/Gaming難」を
  数値で裏付けられた。
- fold別の寄与率にはばらつきがある（時代0.0%は全fold共通、チャンネル効果は
  20.7〜47.4%、内容効果は-1.6〜15.2%）。fold3では内容モデルが僅かに悪化（-1.6%）
  しており、常に内容特徴が効くわけではないことも分かる。

### つまずき・疑問
- `likes_shrinkage.py` の初回実装は4モデル一括のRF学習が45秒制限に収まらずtimeout。
  `likes_rolling_eval.py` 同様に前処理(prepare)とモデル別fit(current/shrink/shrink_strict/content)
  →assembleの分割実行に書き直して解決（npz/pickleキャッシュで前処理を1回に集約）。
- 新セッションでscipy/scikit-learn等の再インストールが必要だった（pip --break-system-packages、
  毎回発生する既知の運用コスト）。
- 最終週（week30）が部分データで n_test=40 と小さく、fold5の指標はノイズが大きい。
  今後の集計では「参考値」として注記し、主要な結論はfold0〜4で語るのが妥当（方向性1の
  分解でもfold5は対象から除外した）。
- `likes_decompose.py` も当初80/20一発分割で実装したが、時代効果の寄与率が数学的に
  常に0%になる問題が発覚し、rolling-origin基盤に乗せる方式へ設計変更（詳細は上記）。

### 方向性7: ordinal分類・分位点回帰（順序情報の活用）— 続けて着手
- 現行D4の4段階分類はRandomForestClassifierで段階を名義（順序なし）クラスとして
  学習している。順序情報を使う2方式を追加・比較（`likes_ordinal.py`、rolling-origin
  基盤fold0〜4）: ordinal(3つの累積二値分類器→argmax) / quantile
  (GradientBoostingRegressorのquantile lossでlog時間相対比の中央値を回帰し、
  D4と同じ学習分位点境界で段階化。10%分位点も回帰し最低保証の下ごしらえとして保存)。

| method | acc平均 | adj1平均 | macroF1平均 | 低F1平均 |
|---|---|---|---|---|
| D4基準(名義4クラスRF) | 0.477 | 0.851 | 0.453 | 0.423 |
| ordinal(累積二値分類×3) | 0.470 | 0.846 | 0.448 | 0.426 |
| quantile(GBR分位点回帰+閾値切り) | 0.474 | 0.857 | 0.429 | 0.292 |

- **合格基準は未達**: ordinalはadj1が僅かに悪化・低F1はほぼ横ばい。quantileはadj1が
  僅かに改善するが低F1が大きく悪化（点推定を閾値で切ると少数クラスの復元力が失われる）。
  「順序を使えば安く効くはず」という仮説はこのデータでは**効かなかった**と正直に記録する
  （ロードマップの鉄則5に従う）。
- **副産物の発見**: quantileモデルの10%分位点（最低保証の候補）の実測カバレッジは
  平均77.4%（目標90%）と大きく未達。GBRのquantile lossは訓練分布への漸近的な性質しか
  持たず、分布シフトのあるこのデータでは較正が効かないことを実証。
  → 方向性3(Conformal Prediction)による理論的な較正が必要という根拠を実証的に補強できた。

### つまずき・疑問（続き）
- GradientBoostingRegressorはスパース行列を直接受け付けないため、fold毎に
  X_tr/X_teを`.toarray()`で密行列化する必要があった（特徴量311次元程度なので
  メモリ上は問題なし）。

### 次にやること
- [ ] ロードマップの次のステップ: **方向性3（conformalで最低保証）**に着手。
      方向性7で判明した「quantile回帰の10%分位点は較正されていない(実測77.4%<目標90%)」
      という結果が、conformalによる理論的較正の必要性を直接裏付けている。
- [ ] 方向性2はrolling-origin基盤（fold別）でも頑健か、余裕があれば追試するとなお良い
      （今回は従来通りの80/20評価のみ）。
- [ ] 方向性1で「内容効果が効かない」と分かったGaming・News&Politicsについて、
      方向性5(レジーム転換の解剖)・9(選択バイアスの明示)と絡めて追加分析する価値がある。

### 生成したファイル
- `likes_rf_genre_log/likes_rolling_eval.py`（rolling-origin評価・分割実行対応）
  - `likes_rolling_eval_folds.csv` / `_summary.csv` / `_plot.png`
- `likes_rf_genre_log/likes_shrinkage.py`（チャンネル効果の経験ベイズ縮小推定・分割実行対応）
  - `likes_shrinkage_k_tuning.csv` / `_group_compare.csv` / `_plot.png`
- `likes_rf_genre_log/likes_decompose.py`（log-likes加法分解・rolling-origin基盤に統合・分割実行対応）
  - `likes_decompose_contrib.csv` / `_by_fold.csv` / `_by_category.csv` / `_plot.png`
- `likes_rf_genre_log/likes_ordinal.py`（ordinal分類・分位点回帰・rolling-origin基盤・分割実行対応）
  - `likes_ordinal_folds.csv` / `_summary.csv` / `_floor.csv`
- `README.md` に Phase8・Phase9・Phase10・Phase11・スクリプト表の行を追記
- `CLAUDE.md` の「次にやること」を更新
- `ChatGPT壁打ちメモ_20260707.md` / `_2.md`（ChatGPTとの壁打ち2件を本プロジェクトの
  文脈に整理して保存。前提のズレの注記付き）

### 外部(Gemini)との壁打ちレビュー（追記）
- 追加で5件の壁打ちメモを保存: `ChatGPT壁打ちメモ_20260707_Gemini解析.md`
  （GeminiによるChatGPTメモの評価。本プロジェクトを総合85〜90点/100点、
  「勝ち確の一歩手前」と評価）、`Gemini壁打ちメモ_20260707.md` / `_完全版.md` /
  `_AI活用の限界と可能性.md` / `_実装ロードマップ.md`（Phase1(RF)→Phase2(DL)の
  検証ロードマップ、コスパ重視/ロマン極限の分岐提示）。
- **前提のズレを確認**: Gemini壁打ちメモ4件は「likesの絶対数を投稿時点で数値回帰予測する」
  ことを最終ゴールとしているが、本プロジェクトは第10回で「投稿前のlikes絶対数予測は難しい」
  と結論し段階分類・時間相対比ターゲットへ転換済み。ChatGPTメモの「LightGBM回帰でR²最大化」
  と同種のズレのため、絶対数回帰への回帰を促す記述はそのまま採用しない
  （Gemini解析メモ自体はこのズレを認識していない点にも注意）。
- **採用に値する部分**: 方向性10（LLMメタ特徴量、クリックベイト度等0-10スコア化）は
  新規ch群のコールドスタート対策として方向性3の次点候補。方向性3(Conformal Prediction)の
  実装詳細（較正セット20%分割・誤差分位点による下限計算・直近N週の適応的較正窓・
  Mondrian個別較正）は現行ロードマップの次ステップにそのまま活用できる具体案として有用。
  他国(CA/GB)データによるチャンネル実績の逆引きは未着手・検討価値あり
  （video_idの国またぎ重複除去が必須）。
- **保留**: CLIP画像特徴量・GNN・週内ランキング学習・FT-Transformer等のDL/マルチモーダル路線は
  ロマン枠（フェーズ3）として保留し、方向性1〜3・7・9の完了を優先する。
- `CLAUDE.md`の「追加の知見（第12回）」「次にやること」を更新。

## 2026-06-30（第11回）

### 今日やったこと
- 第10回の「次にやること」のうちモデル改善2件に集中。
- 4段階分類の「低」段階の崩れを、ビン境界の切り直しで改善（`likes_4class_v2.py`）。
  4方式を比較: v1=学習分位点 / A=全データ分位点 / B=対数等幅 / C=チャンネル相対比。
- 最低保証モデルを「予測時に下限段階を出力する実用関数 `predict_floor()`」に仕上げた
  （`likes_floor_predict.py`）。conf（安全率）で保証の堅さと情報量を調整可能に。
- 追加検証: チャンネルの知名度（学習期間のトレンド入り本数）で test を層別し、
  予測しやすさを比較（`likes_fame_split.py`）。有名(6本+)/中堅(2-5)/無名(1)/新規。

### 分かったこと / 結果
- **「低」段階が崩れる正体は分布シフト**：test期間のlikes中央値(20,562)は学習期間
  (5,642)の約3.6倍。likes絶対値で境界を引くと未来では大半が上位ビンに寄り、
  「低」が痩せる（v1: support=15, F1=0.125）。**ビンの引き直しだけでは根治しない**。
- **4方式の比較（test）**:
  - v1 学習分位点: 正解率0.616 / 低F1=0.125（support 15）
  - A 全データ分位点: 正解率0.567 / 低F1=0.282（support 37）— 低は改善も全体↓
  - B 対数等幅: 正解率0.767 / 低F1=0.000（support 1）— 全体は高いが低が消滅
  - **C チャンネル相対比**: 低F1=0.320（support 227）で「低」を最も復活。ただし
    全体正解率0.344。問題が「実力比でどれだけ伸びたか」に変わるため難化する。
  → 「低」を救うにはターゲット定義をチャンネル相対比に変えるのが最有効。
    絶対数の4段階を保つなら A（全データ分位点）が無難な妥協点。
- **最低保証の実用化**：`predict_floor()` が新規動画→(下限段階・likes下限値・保証成立率)
  を返す。conf を上げるほど控えめ＝保証↑情報量↓。
  - argmax→0.851 / conf0.5→0.881 / **conf0.7→0.950** / conf0.9→0.994。
  - デフォルトconf=0.7で段階別保証: 低1.00 / 中低0.98 / 中高0.94 / 高0.91。
  - サンプル3動画のデモは全て保証成立（控えめな下限に実際が収まった）。
- **チャンネル知名度別の予測力（fame_split / Spearman・AUC）**:
  - 中堅(2-5本) が最も予測しやすい（ρ=0.751, AUC=0.947）。実績が安定し素直に効く。
  - 有名(6本+) は ρ=0.690 / AUC=0.883。話題性で上下にブレるため中堅に劣る。
  - 無名(1本) は ρ=0.588 / AUC=0.790 と低下。
  - **新規(学習期間に未出現) はほぼ予測不能**（ρ=0.125, AUC=0.602≒コイン投げ）。
    → 「誰が出したか」が支配的で、それが不明な新規chでは投稿前予測が崩壊する、
       という従来知見をチャンネル軸で定量的に裏付け。
  - content(内容のみ)モデルは新規群で full(0.125)を上回る(0.281)。実績がノイズに
    なる新規chでは内容特徴の方がまだマシだが、どちらも実用水準には届かない。

### つまずき・疑問
- 「低」段階の救済はビン境界の問題ではなく分布シフトの問題だった。完全な両立
  （全体正解率も低段階F1も高い）は本データでは難しい。用途で定義を選ぶべき。
- sandboxが別セッションに切り替わるとバックグラウンドプロセスが失われる。
  v2/floorは40秒台で完走するので前景+timeoutで回した。

### 次にやること
- [ ] 中間報告書の続編（3段階リーク＋分類への方向転換＋本日の改善）をWord/PDFに。
- [ ] チャンネル相対比(C)を主軸にした分類を正式版に格上げするか検討。
- [ ] チャンネル特徴を「投稿時点より前の実績のみ」で厳密集計する版を試す。
- [ ] 未活用の投稿前特徴を追加検討：description（概要欄・文字数/URL数）、
      サムネイル画像（thumbnail_link から取得し明るさ/顔/テキスト量を特徴量化）。
      ※2017-18年データのため削除動画が多く生存画像のみ対象。

### 検討メモ（本日の質疑から）
- 元データ16列のうち、投稿前に使えるのは title / channel_title / category_id /
  publish_time / tags / comments_disabled / ratings_disabled / description。
  views / dislikes / comment_count / likes / video_error_or_removed は投稿後の量で使用不可。
- 動画本体の要約は不可：CSVに映像/音声/字幕がなく、Claude自身も動画視聴はできない。
  メタデータ（タイトル+タグ+概要）からの要約列なら生成可能。
- Gemini API は YouTube動画URLを渡して内容要約が可能（CLIは機能リクエスト段階、API/SDKが確実）。
  ただし古いデータは生存動画にしか使えない。
- YouTube Data API（メタ取得）は VSCode（=Python）で動く。videos.list は1リクエスト50件・1ユニットで、
  無料枠1日10,000ユニットなら理論上50万件/日。本データ5,817件は1回で全件取得可能。
  ただし返るのは「現在の」統計で当時のlikesは取れない点に注意。

### 生成したファイル
- `likes_rf_genre_log/likes_4class_v2.py`（4方式のビン境界比較）
  - `likes_4class_v2_compare.csv` / `likes_4class_v2_report.txt` / `likes_4class_v2_confusion.png`
- `likes_rf_genre_log/likes_floor_predict.py`（最低保証の実用関数 predict_floor）
  - `likes_floor_predict_tradeoff.csv` / `likes_floor_predict_demo.csv` / `likes_floor_predict_plot.png`
- `likes_rf_genre_log/likes_fame_split.py`（チャンネル知名度で層別した予測比較）
  - `likes_fame_split_results.csv` / `likes_fame_split_plot.png`

## 2026-06-25（第10回）

### 今日やったこと
- likes予測プロジェクト全体をレビュー。
- カテゴリーのまとめ方を検討し、n<500統合版・意味でまとめる案A(6グループ)/案B(8グループ)を実装。
- 改善案1〜4（リーク排除・評価の信頼性・ablation・モデル比較）をコードで検証。
- リークなし正式版モデル `likes_pretime_rf.py` を作成。
- 評価を時系列分割に変えた `likes_pretime_timesplit.py` を作成。
- 今後の方向性を3つ（精度/分析/問題設定変更）すべてシンプルに検証。
- 「事前確定情報でどこまで細かく刻めるか」を順位予測・多クラス分類で探索。
- 4段階分類の正式モデルを作成。
- 予測を「最低保証（実際≥予測で正解）」として使う設定を検証・正式化。

### 分かったこと / 結果
- **当初のR²=0.99は「逆算」だった**：説明変数 views/dislikes/comment_count は likes と同時に決まる量（リーク）。特徴量重要度の99%を占めていた。
- **ablation**：実スケールR²を上げているのは主に「ジャンル分割」。対数変換は実R²をほぼ上げず、対数R²とMAPE/RMSEを改善する（効く指標が違う）。
- **リークを断つと**：投稿時点特徴量だけでR²≈0.95。ただし重要度の0.84はチャンネル過去平均likesに集中＝「誰が投稿したか」がほぼ全てを決める。
- **チャンネル特徴のリークに注意**：全データ平均だと漏れる。学習fold限定で算出すべき（→正式版で対応）。
- **最大の発見＝3段階のリーク**：当初R²=0.99は ①同時生起変数 ②動画重複（37,236行→ユニーク5,817件）③ランダム分割による未来情報混入、の積み重ね。すべて排して時系列評価すると **真のR²≈0.40（±0.20）**、対数R²はほぼ0。
- 結論：**投稿前のlikes予測は実際には難しい**。これは失敗ではなく、評価設計の重要性を示す誠実な結果。

#### 後半（方向性の探索）で分かったこと
- **3方向の探索**：①TF-IDFテキスト特徴を足すとR² 0.41→0.45に向上（テキストは効く）。②チャンネル実績を除くとR²がマイナス＝「内容より発信者」が決定的。③問題設定を変えると、エンゲージ率予測はR²≈0.43、**「伸びるか否か」の二値分類はAUC 0.82**と実用的。
- **カテゴリ別の予測しやすさが激しく違う**：Sports(0.87)/Education(0.79)は当てやすく、Entertainment(-0.11)/Gaming(-0.47)は予測不能。
- **刻み幅の限界**：ピタリ正解率は3段階69% / 4段階62% / 5段階52% / 10段階32%。±1段階許容なら3段階97%・4段階91%・5段階88%。順位予測のSpearman ρ=0.62。→ 4〜5段階が刻みと精度のバランス点。
- **4段階分類（正式版）**：正解率61.6%（ランダム25%）、±1段階91.1%。「高」段階はprecision0.76と良いが、「低」段階はトレンド動画に低likesが少なく崩れる。TF-IDF全体の重要度0.51でテキストの寄与が大きい。
- **最低保証という使い方**：予測段階を「下限」とみなし実際≥予測を正解とすると、成立率は3段階88% / 5段階83% / 10段階76%と、刻んでも落ちにくい。特に下位段階を下限にすれば10段階でも99〜100%保証。「最低でもこの段階は伸びる」という控えめな約束として有用。

### つまずき・疑問
- MAPEが539%になる → likes2桁以下の動画（約990件）でパーセント誤差が爆発するため。主指標はR²/MAEにし、MAPEは小likes動画で不安定と注記する。
- 検証時、計算が重く木の本数を一部40本に削減（結論は不変）。最終報告用は100〜200本で回し直す。

### 次にやること
- [ ] この3段階リークの発見＋方向性検証を中間報告書の続編としてまとめる（Word/PDF）。
- [ ] 4段階分類の「低」段階の崩れを、ビン境界の切り直し等で改善する。
- [ ] チャンネル特徴を「投稿時点より前の実績のみ」で厳密に集計する版を試す。
- [ ] 最低保証モデルを「予測時に下限段階を出力する」実用関数として仕上げる。

### 生成したファイル
- `likes_rf_genre_log/likes_rf_genre_log_merged.py`（n<500統合）
- `likes_rf_genre_log/likes_rf_genre_log_groupA.py` / `groupB.py`（意味でまとめる）
- `likes_rf_genre_log/verify_improvements.py`（改善案1〜4の検証）
- `likes_rf_genre_log/likes_pretime_rf.py`（リークなし正式版）
- `likes_rf_genre_log/likes_pretime_timesplit.py`（時系列評価版）
- `likes_rf_genre_log/explore_three_directions.py`（3方向の探索）
- `likes_rf_genre_log/explore_granularity.py`（刻み幅の限界探索）
- `likes_rf_genre_log/likes_4class.py`（4段階分類の正式モデル）
- `likes_rf_genre_log/likes_floor_guarantee.py`（最低保証モデル）
